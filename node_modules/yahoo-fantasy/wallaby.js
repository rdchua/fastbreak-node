module.exports = function(wallaby) {
  return {
    files: ["**/*.mjs"],
    tests: ["tests/**/*.spec.js"],
    testFramework: "jasmine",
    env: {
      type: "node",
      runner: "node",
      params: {
        runner: `-r ${require.resolve("esm")}`
      }
    }
  };
};
